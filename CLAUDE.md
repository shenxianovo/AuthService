## Agent skills

### Issue tracker

本地 Markdown 文件，issue 存放在 `.scratch/` 目录下。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认标签词汇（needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix）。详见 `docs/agents/triage-labels.md`。

### Domain docs

单一上下文布局，ADR 在 `docs/` 目录下。详见 `docs/agents/domain.md`。
